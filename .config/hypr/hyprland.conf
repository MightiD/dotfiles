################
### MONITORS ###
################

monitor = DP-2, 1920x1080@60, 0x0, 1
monitor = DP-4, 1920x1080@60, 1920x0, 1
monitor = DP-3, 1920x1080@60, 3840x-444, 1, transform, 3
monitor = eDP-1, disable

# PLUGINS
plugin {
    hyprsplit {
        num_workspaces = 10
    }
}


###################
### MY PROGRAMS ###
###################

$terminal = kitty
$fileManager = EDITOR=nvim kitty -e spf
$fileManager2 = dolphin ~
$menu = fuzzel
$browser = chromium


#################
### AUTOSTART ###
#################

exec-once = waybar
exec-once = awww-daemon
exec-once = swaync
exec-once = hypridle
exec-once = hyprpm reload -n
exec-once = hyprctl dispatch workspace 21

exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"
exec = gsettings set org.gnome.desktop.interface gtk-theme "Breeze-Dark"


#############################
### ENVIRONMENT VARIABLES ###
#############################

env = HYPRCURSOR_SIZE,24
env = XCURSOR_SIZE,24

env = GTK_THEME,Breeze-Dark
env = GTK_APPLICATION_PREFER_DARK_THEME,1
env = XDG_CURRENT_DESKTOP,Hyprland
env = QT_QPA_PLATFORM,wayland
env = QT_QPA_PLATFORMTHEME,qt6ct
env = XWAYLAND_FORCE,1

env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = LIBVA_DRIVER_NAME,nvidia
env = NVD_BACKEND,direct


#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 1
    gaps_out = 2

    border_size = 2

    col.active_border = rgb(b5cdd7)

    resize_on_border = true

    allow_tearing = false

    layout = dwindle
}

decoration {
    rounding = 4
    rounding_power = 10

    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

animations {
    enabled = no
}

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = 0 
    disable_hyprland_logo = true 
}


#############
### INPUT ###
#############

input {
    kb_layout = gb
    kb_variant =
    kb_model =
    kb_options = caps:escape
    kb_rules =
    numlock_by_default = true

    follow_mouse = 1

    sensitivity = 0 

    touchpad {
        natural_scroll = true
    }
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

$mainMod = SUPER # Sets "Windows" key as main modifier

# GENERAL FUNCTIONS#
# Apps
bind = $mainMod, return, exec, $terminal
bind = $mainMod, tab, exec, $terminal
bind = $mainMod, E, exec, $fileManager
bind = $mainMod SHIFT, E, exec, $fileManager2
bind = $mainMod, R, exec, $menu
bind = $mainMod, B, exec, $browser
bind = $mainMod, m, exec, hyprlock
bind = $mainMod SHIFT, V, exec, kitty --class clipse -e 'clipse'
bind = $mainMod SHIFT, S, exec, hyprshot -m region output --clipboard-only --freeze

# Hypr functions
bind = $mainMod, Q, killactive
bind = $mainMod, V, togglefloating
bind = $mainMod, S, togglesplit, # dwindle
bind = $mainMod, F,fullscreen
bind = $mainMod CONTROL SHIFT, M, exit


# NAVIGATION #
# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move windows
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

# Movement Between Workspaces
bind = $mainMod CONTROL, k, split:workspace, r+1
bind = $mainMod CONTROL, j, split:workspace, r-1
bind = $mainMod CONTROL SHIFT, k, split:movetoworkspace, r+1
bind = $mainMod CONTROL SHIFT, j, split:movetoworkspace, r-1


# RESIZE WINDOWS #
bindel = $mainMod ALT, h, resizeactive, -15 0
bindel = $mainMod ALT, l, resizeactive, 15 0
bindel = $mainMod ALT, k, resizeactive, 0 -15
bindel = $mainMod ALT, j, resizeactive, 0 15

bindel = $mainMod CONTROL ALT, h, resizeactive, -45 0
bindel = $mainMod CONTROL ALT, l, resizeactive, 45 0
bindel = $mainMod CONTROL ALT, k, resizeactive, 0 -45
bindel = $mainMod CONTROL ALT, j, resizeactive, 0 45

# Resize with mouse
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


# WORKSPACES #
# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, split:workspace, 1
bind = $mainMod, 2, split:workspace, 2
bind = $mainMod, 3, split:workspace, 3
bind = $mainMod, 4, split:workspace, 4
bind = $mainMod, 5, split:workspace, 5
bind = $mainMod, 6, split:workspace, 6
bind = $mainMod, 7, split:workspace, 7
bind = $mainMod, 8, split:workspace, 8
bind = $mainMod, 9, split:workspace, 9
bind = $mainMod, 0, split:workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, split:movetoworkspace, 1
bind = $mainMod SHIFT, 2, split:movetoworkspace, 2
bind = $mainMod SHIFT, 3, split:movetoworkspace, 3
bind = $mainMod SHIFT, 4, split:movetoworkspace, 4
bind = $mainMod SHIFT, 5, split:movetoworkspace, 5
bind = $mainMod SHIFT, 6, split:movetoworkspace, 6
bind = $mainMod SHIFT, 7, split:movetoworkspace, 7
bind = $mainMod SHIFT, 8, split:movetoworkspace, 8
bind = $mainMod SHIFT, 9, split:movetoworkspace, 9
bind = $mainMod SHIFT, 0, split:movetoworkspace, 10


# HYPR XF86 FUNCTIONS #
# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous


##############################
### WINDOWS AND WORKSPACES ###
##############################

# Ignore maximize requests from apps
windowrule = match:class *, suppress_event maximize

windowrule {
    name = kitty-startup

    match:title = Welcome
    match:class = kitty

    float = on
    size = 1000 600
    center = on
}

windowrule = match:title ^(sxiv), tile on

